---
name: skill-audit
description: Show the skill-usage and file-edit audit timeline for the current Codex session. Use when the user asks to inspect, report, or review the session's skill audit.
---

# Skill Audit

Resolve this skill's directory, then run `../../scripts/skill-audit report` from that directory.

Show the command output verbatim in a fenced code block. Do not summarize, reorder, or omit lines.
If it reports no events, state that plainly.

This consumes a model turn. For a zero-token view, tell the user they can run `skill-audit status`
or `skill-audit watch` in a separate terminal.
